# O expo-router puxa um peer (react-server-dom-webpack) que o npm 7+ recusa por
# padrão, abortando `npm install` com ERESOLVE. Este flag replica a resolução usada
# para gerar o package-lock.json, deixando `npm install` e `npm ci` funcionarem.
legacy-peer-deps=true
